# UI Components

Reusable, presentational components. Keep them dumb and style-focused.

- Buttons, inputs, cards, modals, dropdowns
- Use Tailwind/Bootstrap for styling
- Export from index for clean imports: `import { Button } from '@/components/ui'`
