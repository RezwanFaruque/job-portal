# Components

```
components/
├── ui/          # Reusable primitives (Button, Input, Card)
├── layout/      # App shell (Header, Footer, Sidebar)
└── features/    # Feature-specific components (JobCard, ApplicationForm)
```

Import via: `import { X } from '@/components/ui'` or `@/components/layout`.
