"use client";

/**
 * Client providers at app entry point.
 * Add QueryClient, AuthProvider, ThemeProvider, etc. here.
 */
export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
